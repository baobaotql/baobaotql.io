# 银龄金融安全助手

面向老年群体的适老化 AI 金融反诈风险识别微信小程序（前端）。

## 技术栈

- UniApp + Vue 3
- 微信小程序端

## 功能模块（当前为前端 Mock 版）

| 模块 | 说明 |
|------|------|
| 首页 | 拍照识别、相册上传、历史记录、帮助中心入口 |
| 识别流程 | 选图 → 模拟 OCR → 模拟 AI 分析 → 结果页 |
| 结果页 | 风险等级、五项输出、高危词高亮、语音播报 UI |
| 历史记录 | 本地存储、单条/全部删除、详情回看 |
| 帮助中心 | 使用教程、防骗知识库 |
| 隐私授权 | 首次使用弹窗确认 |

## 适老化设计

- 正文 ≥ 18px（36rpx），标题/按钮 ≥ 24px（48rpx）
- 绿/黄/红三色风险等级，高对比配色
- 大按钮、宽间距、极简单页交互
- 结果页自动触发语音播报（微信同声传译插件 TTS）

## 快速开始

### 1. 安装依赖

```bash
npm install
```

### 2. 配置微信小程序 AppID

编辑 `src/manifest.json`，在 `mp-weixin.appid` 填入你的小程序 AppID。

### 2.1 开通语音播报插件（必做）

语音播报依赖微信官方「**微信同声传译**」插件，需在微信公众平台添加：

1. 登录 [微信公众平台](https://mp.weixin.qq.com/)
2. **设置** → **第三方设置** → **插件管理** → **添加插件**
3. 搜索「**微信同声传译**」并添加（AppID：`wx069ba97219f66d99`）
4. 重新编译小程序后，在微信开发者工具中**重新导入** `dist/dev/mp-weixin`

> 注意：该插件通常要求**非个人主体**小程序；测试号可能无法使用，会提示添加插件。

### 3. 开发运行

```bash
npm run dev:mp-weixin
```

用微信开发者工具打开 `dist/dev/mp-weixin` 目录。

### 4. 生产构建

```bash
npm run build:mp-weixin
```

## 项目结构

```
├── src/
│   ├── pages/
│   │   ├── index/          # 首页
│   │   ├── analyzing/      # 识别中
│   │   ├── result/         # 结果页
│   │   ├── history/        # 历史记录
│   │   └── help/           # 帮助中心
│   ├── components/         # 公共组件
│   ├── utils/              # 工具与 Mock 数据
│   ├── uni.scss            # 适老化全局样式变量
│   ├── pages.json
│   └── manifest.json
├── package.json
└── vite.config.js
```

## Mock 说明

当前 OCR 与 AI 分析均为本地 Mock（`utils/mock.js`），会根据模拟文本关键词返回不同风险等级，便于 UI 联调。对接后端时替换 `pages/analyzing/analyzing.vue` 中的调用即可。

## 语音播报说明

- 使用微信同声传译插件 `textToSpeech` 合成语音
- 长文本自动按句分段（每段 ≤48 字）顺序朗读
- 支持播放 / 暂停 / 继续
- 若无声，请确认已添加插件、手机未静音，并在开发者工具 **详情 → 本地设置** 中关闭「静音」相关限制

## 后续对接

- [ ] 对接 Spring Boot 后端 OCR / AI 接口
- [ ] 云端历史记录同步
